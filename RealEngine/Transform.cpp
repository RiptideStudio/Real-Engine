#include "Transform.h"
#include "Random.h"
#include "Timer.h"
#include "Object.h"

void Transform::Update(float dt)
{
  //angle.x += 0.5 * dt;
  //angle.y += 0.75 * dt;
  //angle.z += dt;
}

Transform::Transform()
{
  //scale.x = RandomRangeFloat(0, 5);
  //scale.y = RandomRangeFloat(0, 5);
  //scale.z = RandomRangeFloat(0, 5);
  //angle.x = RandomRangeFloat(0, 360);
  //angle.y = RandomRangeFloat(0, 360);
  //angle.z = RandomRangeFloat(0, 360);
}
